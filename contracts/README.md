# PrivatePhuket API contracts

Canonical contract for the current release candidate:

- `openapi-v1.0-rc5.yaml` — authoritative RC5 contract.

Historical contract retained for traceability:

- `openapi-v0.6.yaml` — superseded staging-baseline contract; do not use for new client generation.

The frontend must not calculate authoritative financial outputs. Calculation, scoring, scenarios, verdict status and publication snapshots come from the backend calculation/publication services.
