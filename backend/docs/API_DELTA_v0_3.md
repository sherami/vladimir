# API Delta v0.3

Added source/evidence/verification endpoints. Before production handoff, merge these paths into canonical `packages/contracts/openapi.yaml` and regenerate frontend client. Source/evidence schemas must preserve DataStatus enum exactly.
